/**
 * Browser-safe subset of the core package. The desktop renderer bundles this entry instead of
 * the Node-dependent index; add modules here when the renderer needs their runtime values.
 */
export * from './chat'
